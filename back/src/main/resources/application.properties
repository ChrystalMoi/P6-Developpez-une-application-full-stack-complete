# Configuration pour MySQL
# spring.datasource.url=jdbc:mysql://${APP_DB_HOST}:${APP_DB_PORT}/${APP_DB_NAME}
# spring.datasource.username=${APP_DB_USER}
# spring.datasource.password=${APP_DB_PASS}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=create
spring.sql.init.mode=never

# Configuration pour MySQL (dev only)
spring.datasource.url=jdbc:h2:mem:devdb
spring.datasource.username=sa
spring.datasource.password=password
spring.datasource.driver-class-name=org.h2.Driver

jwt.secretKey=mySecretKeyForJWT2024mySecretKeyForJWT2024mySecretKeyForJWT2024mySecretKeyForJWT2024
jwt.expirationInMs=3600000

# Swagger v3
springdoc.swagger-ui.path=/swagger-ui.html

# Config serveur
server.port=3000